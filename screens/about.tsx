import { Text, View } from "react-native";
import globlaStyles from '../styles/global'

const About = () => {
    return (
        <View style={globlaStyles.container}>
            <Text style={globlaStyles.titleText}>About Screen</Text>
        </View>
    );
}
 
export default About;